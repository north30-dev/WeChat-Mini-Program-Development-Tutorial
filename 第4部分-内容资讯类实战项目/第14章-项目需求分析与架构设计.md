# 微信小程序开发入门教程

> **适用读者**：具有 Java Web 开发经验（Spring Boot / Spring MVC）的后端开发者
> **学习目标**：从零掌握微信小程序开发，能够独立完成内容资讯类小程序项目
> **教程特色**：Java 视角对比、完整实战项目、企业级开发规范

---

## 第14章 项目需求分析与架构设计

本章将从零开始规划一个"新闻阅读"微信小程序项目，完成需求分析、技术选型、数据库设计和项目结构搭建。对于 Java Web 开发者来说，这就像在动手写代码之前先画好系统的 UML 类图和架构图。

### 14.1 功能清单

| 模块 | 功能点 | 说明 |
|------|--------|------|
| **首页** | 新闻列表 | 按时间倒序展示文章卡片 |
| | 分类导航 | 顶部Tab切换不同分类 |
| | 搜索 | 关键词搜索文章 |
| | 轮播Banner | 首页顶部推荐文章轮播 |
| **详情页** | 富文本阅读 | 渲染HTML格式文章内容 |
| | 评论 | 查看/发表评论 |
| | 收藏 | 收藏/取消收藏文章 |
| | 分享 | 分享给好友或朋友圈 |
| **个人中心** | 登录 | 微信授权登录 |
| | 收藏列表 | 查看已收藏文章 |
| | 阅读历史 | 记录浏览过的文章 |
| | 设置 | 清除缓存、关于我们 |

> **类比理解**：这相当于 Spring Boot 项目中的需求分析阶段。在 Java Web 开发中，你可能会先写一份 PRD（产品需求文档），然后设计 Controller-Service-DAO 三层架构。小程序开发同样需要先理清页面层级和数据流向，只是"视图层"从 Thymeleaf/JSP 变成了 WXML 模板。

### 14.2 技术架构

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              系统技术架构                                        │
├───────────────────────┬───────────────────────┬─────────────────────────────────┤
│                       │                       │                                 │
│   ┌───────────────┐   │   ┌───────────────┐   │   ┌───────────────────────┐   │
│   │  微信小程序前端  │   │   │  Java 后端     │   │   │     微信服务器          │   │
│   │               │   │   │               │   │   │                       │   │
│   │  ┌───────────┐│   │   │  ┌───────────┐│   │   │  微信登录服务          │   │
│   │  │   WXML    ││   │   │  │Controller ││   │   │  wx.login/code2Session │   │
│   │  │   视图层   ││   │   │  │ REST API  ││   │   │                       │   │
│   │  ├───────────┤│   │   │  ├───────────┤│   │   │  微信支付/分享/消息     │   │
│   │  │   WXSS    ││   │   │  │  Service   ││   │   └───────────────────────┘   │
│   │  │   样式层   ││───│───│──│  业务逻辑   ││   │                               │
│   │  ├───────────┤│   │   │  ├───────────┤│   │   ┌───────────────────────┐   │
│   │  │JavaScript ││   │   │  │  Mapper    ││   │   │     第三方服务          │   │
│   │  │   逻辑层   ││   │   │  │  MyBatis   ││   │   │                       │   │
│   │  └───────────┘│   │   │  └──────┬──────┘   │   │  短信/推送/OSS存储     │   │
│   └───────────────┘   │          │          │   └───────────────────────┘   │
│                       │          ▼          │                                 │
│                       │   ┌───────────────┐   │                                 │
│                       │   │    MySQL      │   │                                 │
│                       │   │  用户/文章/    │   │                                 │
│                       │   │  评论/收藏     │   │                                 │
│                       │   └───────────────┘   │                                 │
│                       │                       │                                 │
└───────────────────────┴───────────────────────┴─────────────────────────────────┘

登录流程：小程序 wx.login() → code → Java后端 → code+AppID+AppSecret → 微信服务器
         → openid+session_key → Java后端生成JWT Token → 返回小程序
```

**架构说明：**

| 层级 | 组件 | 职责 |
|------|------|------|
| **小程序前端** | WXML / WXSS / JavaScript | 页面渲染、用户交互、业务逻辑处理 |
| **微信服务器** | 微信登录服务 | 处理 `wx.login()` 请求，返回 `openid` 和 `session_key` |
| **Java后端** | Controller / Service / Mapper | 业务接口、业务逻辑、数据访问 |
| **数据层** | MySQL | 持久化存储用户、文章、评论等数据 |
| **第三方服务** | 短信/推送/OSS | 扩展业务能力 |

> **Java开发者注意点**：小程序前端通过 `wx.request` 发起 HTTPS 请求，与你在 Java Web 中使用 Axios 或 Fetch 调用 REST API 完全一致。后端仍然是熟悉的 Spring Boot + MyBatis 体系，但**微信登录鉴权**需要特别注意：Java后端需要用小程序传来的 `code`，加上自己的 `AppID` 和 `AppSecret`，向微信服务器换取用户的 `openid` 和 `session_key`，这是与常规 Web 应用最大的区别。

### 14.3 数据库设计

```sql
-- 分类表
CREATE TABLE `categories` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '分类ID',
  `name` VARCHAR(50) NOT NULL COMMENT '分类名称',
  `icon` VARCHAR(255) DEFAULT NULL COMMENT '分类图标URL',
  `sort_order` INT DEFAULT 0 COMMENT '排序值',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章分类表';

-- 用户表
CREATE TABLE `users` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `openid` VARCHAR(64) NOT NULL COMMENT '微信openid',
  `nickname` VARCHAR(100) DEFAULT '' COMMENT '昵称',
  `avatar_url` VARCHAR(500) DEFAULT '' COMMENT '头像URL',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 文章表
CREATE TABLE `articles` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '文章ID',
  `title` VARCHAR(200) NOT NULL COMMENT '标题',
  `summary` VARCHAR(500) DEFAULT '' COMMENT '摘要',
  `content` LONGTEXT COMMENT '正文(HTML格式)',
  `cover_url` VARCHAR(500) DEFAULT '' COMMENT '封面图URL',
  `category_id` BIGINT DEFAULT NULL COMMENT '分类ID',
  `author` VARCHAR(100) DEFAULT '' COMMENT '作者',
  `view_count` INT DEFAULT 0 COMMENT '阅读量',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `comment_count` INT DEFAULT 0 COMMENT '评论数',
  `is_top` TINYINT(1) DEFAULT 0 COMMENT '是否置顶',
  `is_banner` TINYINT(1) DEFAULT 0 COMMENT '是否Banner推荐',
  `status` TINYINT(1) DEFAULT 1 COMMENT '状态: 0-草稿 1-已发布 2-下架',
  `published_at` DATETIME DEFAULT NULL COMMENT '发布时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_status_published` (`status`, `published_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章表';

-- 评论表
CREATE TABLE `comments` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '评论ID',
  `article_id` BIGINT NOT NULL COMMENT '文章ID',
  `user_id` BIGINT NOT NULL COMMENT '评论用户ID',
  `parent_id` BIGINT DEFAULT NULL COMMENT '父评论ID(嵌套评论)',
  `reply_user_id` BIGINT DEFAULT NULL COMMENT '被回复用户ID',
  `content` VARCHAR(1000) NOT NULL COMMENT '评论内容',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `status` TINYINT(1) DEFAULT 1 COMMENT '状态: 0-删除 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_article` (`article_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';

-- 收藏表
CREATE TABLE `favorites` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '收藏ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `article_id` BIGINT NOT NULL COMMENT '文章ID',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_article` (`user_id`, `article_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收藏表';

-- 阅读历史表
CREATE TABLE `reading_history` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `article_id` BIGINT NOT NULL COMMENT '文章ID',
  `read_progress` INT DEFAULT 0 COMMENT '阅读进度(百分比)',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_article` (`user_id`, `article_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='阅读历史表';
```

#### 表结构可视化

**categories（分类表）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | 分类ID |
| `name` | VARCHAR(50) | NOT NULL | 分类名称 |
| `icon` | VARCHAR(255) | | 分类图标URL |
| `sort_order` | INT | DEFAULT 0 | 排序值 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**users（用户表）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | 用户ID |
| `openid` | VARCHAR(64) | NOT NULL, UNIQUE | 微信openid |
| `nickname` | VARCHAR(100) | DEFAULT '' | 昵称 |
| `avatar_url` | VARCHAR(500) | DEFAULT '' | 头像URL |
| `phone` | VARCHAR(20) | | 手机号 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**articles（文章表）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | 文章ID |
| `title` | VARCHAR(200) | NOT NULL | 标题 |
| `summary` | VARCHAR(500) | DEFAULT '' | 摘要 |
| `content` | LONGTEXT | | 正文（HTML格式） |
| `cover_url` | VARCHAR(500) | DEFAULT '' | 封面图URL |
| `category_id` | BIGINT | INDEX | 分类ID → categories.id |
| `author` | VARCHAR(100) | DEFAULT '' | 作者 |
| `view_count` | INT | DEFAULT 0 | 阅读量 |
| `like_count` | INT | DEFAULT 0 | 点赞数 |
| `comment_count` | INT | DEFAULT 0 | 评论数 |
| `is_top` | TINYINT(1) | DEFAULT 0 | 是否置顶 |
| `is_banner` | TINYINT(1) | DEFAULT 0 | 是否Banner推荐 |
| `status` | TINYINT(1) | DEFAULT 1 | 状态：0-草稿 1-已发布 2-下架 |
| `published_at` | DATETIME | INDEX (联合status) | 发布时间 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**comments（评论表）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | 评论ID |
| `article_id` | BIGINT | NOT NULL, INDEX | 文章ID → articles.id |
| `user_id` | BIGINT | NOT NULL, INDEX | 评论用户ID → users.id |
| `parent_id` | BIGINT | | 父评论ID（嵌套评论） |
| `reply_user_id` | BIGINT | | 被回复用户ID → users.id |
| `content` | VARCHAR(1000) | NOT NULL | 评论内容 |
| `like_count` | INT | DEFAULT 0 | 点赞数 |
| `status` | TINYINT(1) | DEFAULT 1 | 状态：0-删除 1-正常 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**favorites（收藏表）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | 收藏ID |
| `user_id` | BIGINT | NOT NULL, UNIQUE(联合) | 用户ID → users.id |
| `article_id` | BIGINT | NOT NULL, UNIQUE(联合) | 文章ID → articles.id |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 收藏时间 |

**reading_history（阅读历史表）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | 记录ID |
| `user_id` | BIGINT | NOT NULL, UNIQUE(联合), INDEX | 用户ID → users.id |
| `article_id` | BIGINT | NOT NULL, UNIQUE(联合) | 文章ID → articles.id |
| `read_progress` | INT | DEFAULT 0 | 阅读进度（百分比） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 首次阅读时间 |
| `updated_at` | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 最后阅读时间 |

#### 表关系图

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              数据库表关系                                    │
│                                                                            │
│    ┌─────────────┐          ┌─────────────────┐          ┌─────────────┐     │
│    │ categories  │  1 : N   │    articles      │  1 : N   │   users     │     │
│    │─────────────│◄─────────│─────────────────│─────────►│─────────────│     │
│    │ id (PK)     │          │ id (PK)         │          │ id (PK)     │     │
│    │ name        │          │ title           │          │ openid      │     │
│    │ icon        │          │ content         │          │ nickname    │     │
│    │ sort_order  │          │ category_id(FK) │          │ avatar_url  │     │
│    └─────────────┘          │ author_id (FK)  │          │ created_at  │     │
│                            │ cover_image     │          └──────┬──────┘     │
│                            │ status          │                 │            │
│                            └───────┬─────────┘                 │            │
│                                    │                           │            │
│                    ┌───────────────┼───────────────┐           │            │
│                    │               │               │           │            │
│                    ▼               ▼               ▼           ▼            │
│           ┌─────────────┐ ┌─────────────┐ ┌──────────┐ ┌──────────┐     │
│           │  comments    │ │  favorites   │ │ reading  │ │ reading  │     │
│           │─────────────│ │─────────────│ │ _history │ │ _history │     │
│           │ id (PK)     │ │ id (PK)     │ │ (from    │ │ (from    │     │
│           │ article_id  │ │ article_id  │ │  users)  │ │ articles)│     │
│           │ user_id     │ │ user_id     │ │          │ │          │     │
│           │ content     │ │ created_at  │ │          │ │          │     │
│           │ parent_id   │ └─────────────┘ └──────────┘ └──────────┘     │
│           └─────────────┘                                                       │
│                                                                            │
│  图例：PK=主键  FK=外键  1:N=一对多关系                                       │
└──────────────────────────────────────────────────────────────────────────────┘
```

> **Java开发者注意点**：以上建表 SQL 与你在 Spring Boot 项目中使用的完全一致。`LONGTEXT` 用于存储 HTML 格式的文章正文，`utf8mb4` 字符集确保能存储 Emoji 表情。注意 `favorites` 表的联合唯一索引 `uk_user_article`，这与 Java 中 `@Table(uniqueConstraints = ...)` 注解的作用相同。

### 14.4 项目目录结构

```
news-app/
├── app.js                    # 小程序入口（全局生命周期、全局数据）
├── app.json                  # 全局配置（页面路由、tabBar、窗口样式）
├── app.wxss                  # 全局样式
├── project.config.json       # 项目配置
├── sitemap.json              # 站点地图
├── utils/
│   ├── request.js            # 封装wx.request（类似Axios拦截器）
│   ├── auth.js               # 登录鉴权工具
│   └── util.js               # 通用工具函数
├── components/               # 自定义组件
│   ├── news-card/            # 新闻卡片组件
│   │   ├── news-card.js
│   │   ├── news-card.json
│   │   ├── news-card.wxml
│   │   └── news-card.wxss
│   ├── comment-item/         # 评论项组件
│   └── empty-state/          # 空状态占位组件
├── pages/
│   ├── index/                # 首页
│   │   ├── index.js
│   │   ├── index.json
│   │   ├── index.wxml
│   │   └── index.wxss
│   ├── detail/               # 文章详情页
│   ├── search/               # 搜索页
│   ├── profile/              # 个人中心
│   ├── favorites/            # 收藏列表
│   └── history/              # 阅读历史
└── static/                   # 静态资源
    ├── images/
    └── icons/
```

> **类比理解**：这个目录结构可以类比为 Spring Boot 项目的标准分层：
> - `app.js` ≈ `Application.java`（启动入口）
> - `app.json` ≈ `application.yml`（全局配置）
> - `pages/` ≈ `controller/` + `templates/`（每个页面 = 一个Controller + 一个模板）
> - `utils/` ≈ `util/`（工具类）
> - `components/` ≈ `common/` 下的公共组件或 Thymeleaf Fragment

---

